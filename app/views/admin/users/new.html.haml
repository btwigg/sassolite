#breadcrumb
  = link_to "Back", admin_users_path

%h2 New User

= form_for :user, :url => admin_users_path do |f|
  = f.error_messages

  = f.label :login
  %br
  = f.text_field :login
  %br
    
  = f.submit